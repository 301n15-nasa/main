<!DOCTYPE html>
<html lang="en">
  <head>
      <%- include ('../../partials/head') %>
    <title>Asteroids - NeoWs API</title>
  </head>
  <body>
    <header>
      <%- include ('../../partials/header') %>
    </header>
    <h1>ASTEROIDS</h1>
    <main>
      <section class="asteroids">
        <% if(results) { %>
          <% results.forEach( asteroid => { %>
            <div class="asteroid flex-column">
              <h3> <span class="show">Date of closest approach: </span><%= asteroid.date; %> </h3>
              <h4> <span class="show">Diameter: </span><%= asteroid.meters; %> meters (<%= asteroid.feet; %> feet)  </h4>
              <h4> <span class="show">miss distance: </span><%= asteroid.miss_distance_km; %> kilometers (<%= asteroid.miss_distance_km; %> miles)  </h4>
              <p> <span class="show">Hazardous: </span><%= asteroid.hazardous; %> </p>
              <button class="view_btn"><a href="<%= asteroid.link; %>" target="blank"> View orbit diagram</a></button>
              <button class="select button">Select</button>
              <div class="modal">
                <div class="asteroid">
                  <%- include('../../partials/form', { type: 'search', asteroid: asteroid}) %>
                </div>
              </div>
            </div>
          <% }); %>
        <% } else { %>
          <div class="flex-column">
            <h3> No asteroid found. Please refine search criteria and <a href="./">try again</a> </h3>
          </div>
        <% }; %>
      </section>
    </main>
    <footer>
        <%- include ('../../partials/footer') %>
    </footer>
    <script src="https://code.jquery.com/jquery-3.4.1.min.js" integrity="sha256-CSXorXvZcTkaix6Yvo6HppcZGetbYMGWSFlBw8HfCJo=" crossorigin="anonymous">
    </script>
    <script src="../../js/menu.js"></script>
    <script src="../../js/app.js"></script>
  </body>
</html>
